{
  "scripts": {
    "start": "nf start",
    "client": "elm-live src/BracketBuilder.elm --start-page=dev.html --open -- --output=dev.js --debug",
    "server": "json-server --watch db.json"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-live": "^4.0.2",
    "foreman": "^3.0.1",
    "json-server": "^0.17.0"
  }
}
